import React from 'react'

export const metadata = {
    title: 'Dashboard',
    description: 'Dashboard Page',
}

const Dashboard = () => {
    return (
        <div>Dashboard</div>
    )
}

export default Dashboard